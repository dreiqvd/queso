{
  "name": "wedding",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "app",
  "sourceRoot": "apps/wedding/src",
  "tags": ["type:app", "scope:wedding"],
  "targets": {
    "build": {
      "executor": "@angular/build:application",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/wedding",
        "browser": "apps/wedding/src/main.ts",
        "polyfills": ["zone.js"],
        "tsConfig": "apps/wedding/tsconfig.app.json",
        "inlineStyleLanguage": "scss",
        "assets": [
          {
            "glob": "**/*",
            "input": "apps/wedding/public"
          }
        ],
        "styles": ["apps/wedding/src/styles.scss"],
        "stylePreprocessorOptions": {
          "includePaths": [
            "libs/shared/assets/styles/core",
            "libs/shared/assets/styles/themes"
          ]
        },
        "server": "apps/wedding/src/main.server.ts",
        "ssr": {
          "entry": "apps/wedding/src/server.ts"
        },
        "outputMode": "server"
      },
      "configurations": {
        "production": {
          "budgets": [
            {
              "type": "initial",
              "maximumWarning": "500kb",
              "maximumError": "1mb"
            },
            {
              "type": "anyComponentStyle",
              "maximumWarning": "4kb",
              "maximumError": "8kb"
            }
          ],
          "outputHashing": "all"
        },
        "development": {
          "optimization": false,
          "extractLicenses": false,
          "sourceMap": true
        }
      },
      "defaultConfiguration": "production"
    },
    "serve": {
      "continuous": true,
      "executor": "@angular/build:dev-server",
      "configurations": {
        "production": {
          "buildTarget": "wedding:build:production"
        },
        "development": {
          "buildTarget": "wedding:build:development",
          "liveReload": true,
          "hmr": true,
          "watch": true
        }
      },
      "defaultConfiguration": "development"
    },
    "extract-i18n": {
      "executor": "@angular/build:extract-i18n",
      "options": {
        "buildTarget": "wedding:build"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/wedding/jest.config.ts"
      }
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "wedding:build",
        "port": 4200,
        "staticFilePath": "dist/apps/wedding/browser",
        "spa": true
      }
    }
  }
}
